enum GenderEnum {
  MALE = 'male',
  FEMALE = 'female',
}

enum PetCategoryEnum {
  DOG = 'dog',
  CAT = 'cat',
  BIRD = 'bird',
  RABBIT = 'rabbit',
  HAMSTER = 'hamster',
  TURTLE = 'turtle',
  FISH = 'fish',
  OTHER = 'other',
}

enum RoleEnum {
  ADMIN = 'admin',
  USER = 'user',
}

export { GenderEnum, PetCategoryEnum, RoleEnum };
